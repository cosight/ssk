ssk:
  jwt:
    secret: foo

logging:
  level:
    root: info

server:
  port: 80

spring:
  application:
    name: ssk

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false

  h2:
    console:
      enabled: true

  redis:
    host: localhost
    port: 6379
    database: 21
    password: "*N2vya7H@muDTwdNMR!"

  profiles:
    group:
      local: "common, local-config, testdb"
      dev: "common, dev-config, devdb"
      prod: "common, proddb"

---

spring:
  config:
    activate:
      on-profile: local-config

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show-sql: true

---

spring:
  config:
    activate:
      on-profile: testdb

  # 테스트 환경에서는 H2 데이터베이스를 MySQL 모드로 사용
  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:~/h2/testdb;MODE=MYSQL
    driver-class-name: org.h2.Driver
    username: sa

---

spring:
  config:
    activate:
      on-profile: devdb

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://133.186.211.156:3306/final_exam4
    username: final_exam4
    password: KKWYKU1JKCTMw]WH
